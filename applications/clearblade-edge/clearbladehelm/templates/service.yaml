apiVersion: v1
kind: Service
metadata:
  name: clearblade-edge2
  labels:
    app: clearblade-edge2
spec:
  type: {{ .Values.serviceType }}
  ports:
    - port: 9000
      protocol: TCP
      targetPort: 9000
      #name: sql
